# Dependencies
node_modules/

# Environment / secrets — NEVER commit these
.env
.env.local
.env.*.local

# Mastra build / runtime output
.mastra/
dist/
build/

# LibSQL / SQLite local databases (runtime state — never commit)
*.db
*.db-journal
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/

# Planning files
PROJECT_PLAN.md

# Claude Code / assistant folders — keep local, not in remote
.claude/
**/.claude/